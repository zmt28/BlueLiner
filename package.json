{
  "name": "blueliner-frontend",
  "private": true,
  "description": "Type-checking + (PR B) build pipeline for the Blueliner web client.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "typescript": "^5.5.0"
  }
}
